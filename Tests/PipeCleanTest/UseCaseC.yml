config:
  plugins:
  target: "https://kwqnze338k.us-east-1.awsapprunner.com"
  phases:
    - duration: 600
      arrivalRate: 1
      name: Pipe Clean

scenarios:
  - name: "Choose only one recipe"
    flow:
      - get:
          url: "/recipes/2"
      - think: 1
