config:
  plugins:
  target: "https://kwqnze338k.us-east-1.awsapprunner.com/"
  phases:
    - duration: 60
      arrivalRate: 1
      name: Pipe Clean

scenarios:
  - name: "All recipes"
    flow:
      - get:
          url: "/recipes"
      - think: 2
