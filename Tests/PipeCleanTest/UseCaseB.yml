config:
  plugins:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 1
      name: Pipe Clean

scenarios:
  - name: "Choose one recipe"
    flow:
      - get:
          url: "/recipes"
      - think: 2
      - get:
          url: "/recipes/2"
      - think: 1
