config:
  plugins:
  target: "http://localhost:3000"
  phases:
    - duration: 200
      arrivalRate: 10
      rampTo: 50
      name: Stress Test

scenarios:
  - name: "All recipes"
    flow:
      - get:
          url: "/recipes"
      - think: 2
